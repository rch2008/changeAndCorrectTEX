Private Sub Command1_Click()

    Dim getdir As String

    getdir = BrowseForFolder(Me, "选择工作文件夹", Text1.Text)

    If Len(getdir) = 0 Then Exit Sub
    Text1.Text = IIf(Right$(getdir, 1) = "\", getdir, getdir & "\")

End Sub